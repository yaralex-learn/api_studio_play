"use client"

// This component is no longer used as we're handling fonts with CSS
// Keeping it as a placeholder in case we need to add JS-based font handling in the future
export function FontProvider() {
  // Font handling is now done via CSS in globals.css
  return null
}
