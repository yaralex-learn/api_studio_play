## Root env for `compose.dev.yml`
## Copy to `.env` for local dev:
##   cp .env.example .env
##
## Used by: play + studio (optional; only needed if you want Google OAuth UI wired up)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=

